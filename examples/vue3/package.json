{
  "name": "partialmesh-peerpigeon-demo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm --prefix ../.. run dev:mdns --",
    "dev:raw": "npm --prefix ../.. run wasm:build && vite --host 0.0.0.0 --port 5173 --strictPort",
    "build": "npm --prefix ../.. run wasm:build && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "freertc": "github:draeder/freertc",
    "peerpigeon": "file:../..",
    "unsea": "^1.1.2",
    "vue": "^3.3.0"
  },
  "devDependencies": {
    "@rollup/plugin-inject": "^5.0.5",
    "@vitejs/plugin-vue": "^5.0.0",
    "buffer": "^6.0.3",
    "events": "^3.3.0",
    "process": "^0.11.10",
    "stream-browserify": "^3.0.0",
    "util": "^0.12.5",
    "vite": "^5.0.0"
  }
}
